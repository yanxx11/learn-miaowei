在删除li的数据时，发现有些数据没删掉，采用的方式是
for(var i=0; i<aLi.length;i++)
{
  oUl.removeChild(aLi[i]);
 }
经过调试发现，
   如果有一个数据正常，
   如果有两个数据剩下第二个没删除，循环执行一次，循环到的aLi的值是第一个，没有遍历到第二个数据
   如果三个数据，剩下第二个没删除，循环执行两次，循环到的aLi的值是第一个和第三个，没有遍历到第二个数据
   这时我突然悟到，原来是删除第一个数据，原来第二个位置的索引立刻变成0
解决方案:倒叙法
for(var i=aLi.length-1;i>=0;i--)
{
	oUlChild.removeChild(aLi[i]);
}
